# 快速测试指南 - UI 和飞书信息共用

## 启动服务器

```bash
cd "E:\AI_projects\scholar-claw-feishu -1.0.0.5"
pnpm run build
pnpm start
```

预期输出：
```
ScholarClaw running at http://localhost:18789 (Model: qwen3.5-plus)
[Feishu] WebSocket client started successfully  (如果配置了飞书)
```

## 测试场景

### 场景 1: 文献共享测试

**步骤 1 - Web UI 上传文献**:
1. 打开浏览器：http://localhost:18789
2. 上传一个文献文件（.txt/.ris/.bib 格式）
3. 等待上传完成提示

**步骤 2 - 飞书查询文献**:
1. 在飞书中给机器人发送："我上传了哪些文献？"
2. 预期回复：应该能看到在 Web UI 中上传的文献列表

**验证点**: 
- ✅ 飞书能访问 Web UI 上传的文献
- ✅ 文献数量、标题等信息一致

---

### 场景 2: 写作进度共享测试

**步骤 1 - 飞书创建草稿**:
1. 在飞书中发送："帮我写引言"
2. AI 会询问写作重点
3. 回复写作重点，AI 生成内容并保存草稿

**步骤 2 - Web UI 查看草稿**:
1. 在 Web UI 中查看草稿列表
2. 预期能看到在飞书中创建的"引言"草稿

**验证点**:
- ✅ Web UI 显示飞书创建的草稿
- ✅ 草稿内容完全一致

---

### 场景 3: 记忆同步测试

**步骤 1 - Web UI 建立记忆**:
1. 在 Web UI 中发送："我想研究华北平原 N2O 排放对气候变化的影响"
2. AI 回复后，记忆系统会保存这个研究主题

**步骤 2 - 飞书查询记忆**:
1. 在飞书中发送："我的研究主题是什么？"
2. 预期回复：应该能提到"华北平原 N2O 排放"

**验证点**:
- ✅ 飞书能回忆起 Web UI 中提到的研究主题
- ✅ 记忆内容准确

---

### 场景 4: 会话历史共享测试

**步骤 1 - Web UI 对话**:
1. 在 Web UI 中进行多轮对话
2. 讨论某个具体话题（如实验设计）

**步骤 2 - 飞书继续对话**:
1. 在飞书中发送相关话题
2. AI 应该能结合之前的对话历史回复

**验证点**:
- ✅ AI 能理解跨平台的对话上下文
- ✅ 不会重复询问已在 Web UI 中讨论过的信息

---

## 数据文件位置

所有共享数据存储在以下位置：

```
data/
├── uploads/
│   └── web-user/          # 所有用户上传的文献
│       ├── literature.txt
│       └── literature.json
├── sessions/
│   └── web-user/          # 所有用户的会话草稿
│       └── drafts/
└── memory/
    └── web-user/          # 所有用户的记忆
        ├── memory.json
        └── conversations/
```

## 常见问题

### Q1: 为什么所有用户共用同一个 ID？
**A**: 这是 1.0.0.6 的设计决策，简化了多端体验。如果需要用户隔离，可以修改 `unifiedUserId` 逻辑。

### Q2: 飞书没有响应？
**A**: 检查以下几点：
- 环境变量 `FEISHU_APP_ID` 和 `FEISHU_APP_SECRET` 是否配置
- 飞书 WebSocket 是否正常启动（查看日志）
- 飞书机器人配置是否正确

### Q3: 数据没有同步？
**A**: 检查：
- 确保两个端口都使用同一个服务器实例
- 查看 `data/uploads/web-user/` 目录是否有文件生成
- 检查服务器日志中的用户 ID 是否都是 "web-user"

---

## 日志调试

启动时添加调试参数：
```bash
DEBUG=1 pnpm start
```

关键日志标记：
- `[ChatProcessor]` - 消息处理
- `[GlobalHandler]` - 全局消息处理器
- `[FeishuWS]` - 飞书 WebSocket
- `[Memory]` - 记忆存取

---

**测试完成日期**: ___________  
**测试人员**: ___________  
**测试结果**: □ 通过  □ 失败  □ 部分通过
