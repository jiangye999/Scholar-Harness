# 任务契约（Task Brief）

> 任何复杂任务在写代码 / 调工具之前先填这一段（3-6 行即可）。验收标准写不出来就先把任务问清楚。

- **目标**：
- **输入**：（文件路径、已知参数、数据来源）
- **期望输出**：
- **验收标准**：（如何判断"做完了"——可测试、可观测）
- **约束 / 不触碰**：
- **验证命令**：

## 基准校准（Ground Truth）

- 已知参考点 / 坐标系 / 不变值：
- 校准方式：
- 假设（与测量冲突时优先修正假设）：

## 工具化检查

- [ ] 是否已有可复用工具？（查 `skill-packs/agent-problem-solving/`）
- [ ] 同类操作是否已出现 ≥3 次？（是 → 必须工具化）
- [ ] 输出目录是否已 `os.makedirs(exist_ok=True)`？

## 结论输出契约

```json
{ "status": "success|warning|error", "summary": "一行结论", "next_actions": ["..."], "artifacts": ["文件路径"] }
```
